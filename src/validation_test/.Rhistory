dplyr::select(rownames, ACMG_total_score) |>
filter(ACMG_total_score > 5) |>
unique() |>
nrow()
matches_total
matches_uniq
df_validate$rownames |> unique() |> length()
df_validate$rownames |> length()
# Generate the bar plot
matches_total <- df_validate %>%
dplyr::select(rownames, ACMG_total_score) |>
# filter(ACMG_total_score > 0) |>
unique()|>
nrow()
matches_total
# filter(ACMG_total_score > 0) |>
# unique()|>
# Generate the bar plot
matches_total <- df_validate %>%
dplyr::select(rownames, ACMG_total_score) |>
# filter(ACMG_total_score > 0) |>
# unique()|>
nrow()
matches_total
matches_unfil_total <- df_validate %>%
dplyr::select(rownames, ACMG_total_score) |>
# filter(ACMG_total_score > 0) |>
# unique()|>
nrow()
matches_unfil_uniq <- df_validate %>%
dplyr::select(rownames, ACMG_total_score) |>
# filter(ACMG_total_score > 0) |>
unique()|>
nrow()
matches_unfil_total
matches_unfil_uniq
matches_total <- df_validate %>%
dplyr::select(rownames, ACMG_total_score) |>
filter(ACMG_total_score > 0) |>
unique()|>
nrow()
matches_uniq <- df_validate %>%
dplyr::select(rownames, ACMG_total_score) |>
filter(ACMG_total_score > 5) |>
unique() |>
nrow()
## Count hits ----
# Generate the bar plot
matches_unfil_total <- df_validate %>%
dplyr::select(rownames, ACMG_total_score) |>
nrow()
matches_unfil_uniq <- df_validate %>%
dplyr::select(rownames, ACMG_total_score) |>
unique()|>
nrow()
matches_total <- df_validate %>%
dplyr::select(rownames, ACMG_total_score) |>
filter(ACMG_total_score > 0) |>
unique()|>
nrow()
matches_uniq <- df_validate %>%
dplyr::select(rownames, ACMG_total_score) |>
filter(ACMG_total_score > 5) |>
unique() |>
nrow()
matches_unfil_total
matches_unfil_uniq
matches_total
matches_uniq
View(df_validate)
df_validate %>%
dplyr::select(rownames, ACMG_total_score) %>%
group_by(ACMG_total_score) %>%
summarise(count = n()) %>%
ggplot(aes(x = ACMG_total_score, y = count)) +
geom_bar(stat = "identity", fill= "darkblue", color = "black") +
labs(x = "Total score", y = "Count of variants after new QC/QV\noverlapping with previous study", title = "Count of variants per interpretation score",
subtitle = paste0("Total matches = ", matches_unfil_total,
", unique variants = ", matches_unfil_uniq,
", evidence score >0 = ", matches_total,
", unique variants evidence score >0 = ", matches_uniq
)) +
geom_vline(xintercept = 5.5, linetype = "dotted")
df_validate %>%
dplyr::select(rownames, ACMG_total_score) %>%
group_by(ACMG_total_score) %>%
summarise(count = n()) %>%
ggplot(aes(x = ACMG_total_score, y = count)) +
geom_bar(stat = "identity", fill= "darkblue", color = "black") +
labs(x = "Total score", y = "Count of variants after new QC/QV\noverlapping with previous study", title = "Count of variants per interpretation score",
subtitle = paste0("Total matches = ", matches_unfil_total,
", unique variants = ", matches_unfil_uniq,
",\n evidence score >0 = ", matches_total,
", unique variants evidence score >0 = ", matches_uniq
)) +
geom_vline(xintercept = 5.5, linetype = "dotted")
output_directory
file_suffix
matches_unfil_total
matches_unfil_uniq
matches_total
matches_uniq
subtitle <-  paste0("Total matches = ", matches_unfil_total,
", unique variants = ", matches_unfil_uniq,
",\n evidence score >0 = ", matches_total,
", unique variants evidence score >0 = ", matches_uniq
)
subtitle
subtitle_text <-  paste0("Total matches = ", matches_unfil_total,
", unique variants = ", matches_unfil_uniq,
",\n evidence score >0 = ", matches_total,
", unique variants evidence score >0 = ", matches_uniq
)
df_validate %>%
dplyr::select(rownames, ACMG_total_score) %>%
group_by(ACMG_total_score) %>%
summarise(count = n()) %>%
ggplot(aes(x = ACMG_total_score, y = count)) +
geom_bar(stat = "identity", fill= "darkblue", color = "black") +
labs(x = "Total score", y = "Count of variants after new QC/QV\noverlapping with previous study", title = "Count of variants per interpretation score",
# subtitle = subtitle_text
) +
geom_vline(xintercept = 5.5, linetype = "dotted")
ggsave(paste("../../images/", output_directory, file_suffix, "validation_check_score.pdf", sep = "") ,plot = p.acmg_score )
p_df_validate <- df_validate %>%
dplyr::select(rownames, ACMG_total_score) %>%
group_by(ACMG_total_score) %>%
summarise(count = n()) %>%
ggplot(aes(x = ACMG_total_score, y = count)) +
geom_bar(stat = "identity", fill= "darkblue", color = "black") +
labs(x = "Total score", y = "Count of variants after new QC/QV\noverlapping with previous study", title = "Count of variants per interpretation score",
# subtitle = subtitle_text
) +
geom_vline(xintercept = 5.5, linetype = "dotted")
ggsave(paste("../../images/", output_directory, file_suffix, "validation_check_score.pdf", sep = "") ,plot = p_df_validate )
View(df_validate)
Borghesi_test <- df |>
filter(
(SYMBOL == "WAS" & HGVSp == "ENSP00000365891.4:p.Glu131Lys" & genotype == "2") |
(SYMBOL == "CYBB" & HGVSp == "ENSP00000367851.4:p.Gly364Arg" & genotype == "2") |
(SYMBOL == "CFH" & HGVSp == "ENSP00000356399.4:p.Pro503Ala")
) |>
dplyr::select(sample,
SYMBOL,
rownames,
chr,
HGVSp,
HGVSc,
Consequence,
"IMPACT",
"genotype",
"Inheritance",
"CLIN_SIG",
"gnomAD_AF",
ACMG_total_score
) |>
arrange(SYMBOL,
sample)
Borghesi_test |> dplyr::select(rownames, SYMBOL, sample, genotype, HGVSp, gnomAD_AF, Inheritance, ACMG_total_score) |> unique()
df_validate$rownames |> unique() |> length()
df_validate$rownames |> length()
df_validate |> dplyr::select(rownames, ACMG_total_score)
df_validate <- as.data.frame(df_validate)
class(df_validate$rownames)
## Count hits ----
# Generate the bar plot
matches_unfil_total <- df_validate %>%
dplyr::select(rownames, ACMG_total_score) |>
nrow()
matches_unfil_uniq <- df_validate %>%
dplyr::select(rownames, ACMG_total_score) |>
unique()|>
nrow()
matches_total <- df_validate %>%
dplyr::select(rownames, ACMG_total_score) |>
filter(ACMG_total_score > 0) |>
unique()|>
nrow()
matches_uniq <- df_validate %>%
dplyr::select(rownames, ACMG_total_score) |>
filter(ACMG_total_score > 5) |>
unique() |>
nrow()
matches_unfil_total
matches_unfil_uniq
matches_total
matches_uniq
subtitle_text <-  paste0("Total matches = ", matches_unfil_total,
", unique variants = ", matches_unfil_uniq,
",\n evidence score >0 = ", matches_total,
", unique variants evidence score >0 = ", matches_uniq
)
subtitle_text
library(ggplot2); theme_set(theme_bw())
p_df_validate <- df_validate %>%
dplyr::select(rownames, ACMG_total_score) %>%
group_by(ACMG_total_score) %>%
summarise(count = n()) %>%
ggplot(aes(x = ACMG_total_score, y = count)) +
geom_bar(stat = "identity", fill= "darkblue", color = "black") +
labs(x = "Total score", y = "Count of variants after new QC/QV\noverlapping with previous study", title = "Count of variants per interpretation score",
# subtitle = subtitle_text
) +
geom_vline(xintercept = 5.5, linetype = "dotted")
ggsave(paste("../../images/", output_directory, file_suffix, "validation_check_score.pdf", sep = "") ,plot = p_df_validate )
df_val <- df_val |> dplyr::select(SYMBOL, HGVSp, rownames)
df_borgh$source <- "borgh"
df_val$source <- "law"
head(df_borgh)
head(df_val)
output_directory
file_suffix
subtitle_text <-  paste0("Total matches = ", matches_unfil_total,
", unique variants = ", matches_unfil_uniq,
",\n evidence score >0 = ", matches_total,
", unique variants evidence score >0 = ", matches_uniq
)
subtitle_text
patient_list <- c("BE056", "LU013", "LAU164")
df |> filter(sample %in% patient_list)
df$sample
patient_list <- c("BE_056", "LU_013", "LAU_164")  # Ensure the list is correctly formatted
patient_list <- c("BE_056", "LU_013", "LAU_164")
# Create a regular expression from the patient_list
pattern <- paste(patient_list, collapse="|")
# Filter df where 'sample' partially matches any pattern in patient_list
df_filtered <- df %>%
filter(grepl(pattern, sample))
View(df)
patient_list <- c("BE056", "LU013", "LAU164")
# Create a regular expression from the patient_list
pattern <- paste(patient_list, collapse="|")
# Filter df where 'sample' partially matches any pattern in patient_list
df_filtered <- df %>%
filter(grepl(pattern, sample))
View(df_filtered)
df_filtered$sample
df_filtered$sample  |> unique()
View(df_filtered)
# Filter df where 'sample' partially matches any pattern in patient_list
df_known_patients <- df %>%
filter(grepl(pattern, sample))
df_known_patients$sample  |> unique()
df_report_main_text <- df_known_patients |>
# filter(ACMG_total_score > 5 ) |>
# filter(ACMG_total_score > 9 ) |>
dplyr::select(sample.id,
# ACMG_score,
# ACMG_count,
# ACMG_highest,
SYMBOL,
rownames,
# chr,
HGVSp,
HGVSc,
Consequence,
# IMPACT,
genotype,
Inheritance,
gnomAD_AF,
# comp_het_flag
# list_of_used_columns
ACMG_total_score
) |>
arrange(SYMBOL, sample.id,
desc(ACMG_total_score),
sample.id)
df_report_main_text <- df_known_patients |>
# filter(ACMG_total_score > 5 ) |>
# filter(ACMG_total_score > 9 ) |>
dplyr::select(sample,
# ACMG_score,
# ACMG_count,
# ACMG_highest,
SYMBOL,
rownames,
# chr,
HGVSp,
HGVSc,
Consequence,
# IMPACT,
genotype,
Inheritance,
gnomAD_AF,
# comp_het_flag
# list_of_used_columns
ACMG_total_score
) |>
arrange(SYMBOL, sample.id,
desc(ACMG_total_score),
sample.id)
df_report_main_text <- df_known_patients |>
# filter(ACMG_total_score > 5 ) |>
# filter(ACMG_total_score > 9 ) |>
dplyr::select(sample,
# ACMG_score,
# ACMG_count,
# ACMG_highest,
SYMBOL,
rownames,
# chr,
HGVSp,
HGVSc,
Consequence,
# IMPACT,
genotype,
Inheritance,
gnomAD_AF,
# comp_het_flag
# list_of_used_columns
ACMG_total_score
) |>
arrange(SYMBOL, sample,
desc(ACMG_total_score),
sample)
colnames(df_report_main_text)[colnames(df_report_main_text) == 'ACMG_total_score'] <- 'ACMG score'
colnames(df_report_main_text)[colnames(df_report_main_text) == 'rownames'] <- 'Variant GRCh38'
View(df_report_main_text)
df_report_main_text <-
df_report_main_text |>
arrange(sample,
desc(ACMG_total_score),
SYMBOL,
sample)
df_report_main_text <- df_known_patients |>
# filter(ACMG_total_score > 5 ) |>
# filter(ACMG_total_score > 9 ) |>
dplyr::select(sample,
# ACMG_score,
# ACMG_count,
# ACMG_highest,
SYMBOL,
rownames,
# chr,
HGVSp,
HGVSc,
Consequence,
# IMPACT,
genotype,
Inheritance,
gnomAD_AF,
# comp_het_flag
# list_of_used_columns
ACMG_total_score
) |>
arrange(SYMBOL, sample,
desc(ACMG_total_score),
sample)
df_report_main_text <-
df_report_main_text |>
arrange(sample,
desc(ACMG_total_score),
SYMBOL,
sample)
# Add a new column with the maximum ACMG_total_score for each SYMBOL
df_report_main_text <- df_report_main_text %>%
group_by(SYMBOL) %>%
mutate(max_score = max(ACMG_total_score)) %>%
ungroup()
# Arrange the data frame based on sample, then descending max_score, SYMBOL, and then by desc(ACMG_total_score)
df_report_main_text <- df_report_main_text %>%
arrange(sample, desc(max_score), SYMBOL, desc(ACMG_total_score))
# Arrange the data frame based on sample, then descending max_score, SYMBOL, and then by desc(ACMG_total_score)
df_report_main_text <- df_report_main_text %>%
unique() %>%
arrange(sample, desc(max_score), SYMBOL, desc(ACMG_total_score))
colnames(df_report_main_text)[colnames(df_report_main_text) == 'ACMG_total_score'] <- 'ACMG score'
colnames(df_report_main_text)[colnames(df_report_main_text) == 'rownames'] <- 'Variant GRCh38'
df_x <- df |> filter(Inheritance == XL)
df_x <- df |> filter(Inheritance == "XL")
View(df_x)
df_x <- df |> filter(Inheritance == "XL") |> filter(genotype = 2)
df_x <- df |> filter(Inheritance == "XL") |> filter(genotype == 2)
View(df_x)
df_x <- df |> filter(Inheritance == "XL") |> filter(genotype == 2) |> unique()
View(df_x)
df_x <- df |> filter(Inheritance == "XL") |> filter(genotype == 2) |> distinct()
View(df_x)
df_x <- df |> filter(Inheritance == "XL") |> filter(genotype == 2) |>
dplyr::select(sample,
SYMBOL,
rownames,
HGVSp,
HGVSc,
Consequence,
genotype,
Inheritance,
gnomAD_AF,
ACMG_total_score
) |>
arrange(SYMBOL, sample,
desc(ACMG_total_score),
sample)
df_x <- df |> filter(Inheritance == "XL") |> filter(genotype == 2) |>
dplyr::select(sample,
SYMBOL,
rownames,
HGVSp,
HGVSc,
Consequence,
genotype,
Inheritance,
gnomAD_AF,
ACMG_total_score
) |>
arrange(SYMBOL, sample,
desc(ACMG_total_score),
sample) |>
unique()
View(df_x)
df_x <- df |> filter(Inheritance == "XL") |> filter(genotype == 2) |>
dplyr::select(sample,
SYMBOL,
rownames,
HGVSp,
HGVSc,
Consequence,
genotype,
Inheritance,
gnomAD_AF,
ACMG_total_score
) |>
unique()
# Add a new column with the maximum ACMG_total_score for each SYMBOL
df_x <- df_x %>%
group_by(SYMBOL) %>%
mutate(max_score = max(ACMG_total_score)) %>%
ungroup()
df_x <- df_x %>%
unique() %>%
arrange(sample, desc(max_score), SYMBOL, desc(ACMG_total_score))
df_x$sample |> unique()
df_x |> filter(ACMG_total_score > 5)
df_x |> filter(ACMG_total_score > 5) |> unique(SYMBOL)
df_x |> filter(ACMG_total_score > 5) |> unique(SYMBOL)
df_x |> filter(ACMG_total_score > 5) |> select(SYMBOL) |> unique()
df_x |> filter(ACMG_total_score > 5) |> dfplyr::select(SYMBOL) |> unique()
df_x |> filter(ACMG_total_score > 5) |> dplyr::select(SYMBOL) |> unique()
df_x |> filter(ACMG_total_score > 5) |> dplyr::select(sample) |> unique()
patient_list <- c("BE056", "LU013", "LAU164")
# Create a regular expression from the patient_list
pattern <- paste(patient_list, collapse="|")
# Filter df where 'sample' partially matches any pattern in patient_list
df_known_patients <- df %>%
filter(grepl(pattern, sample))
df_known_patients$sample  |> unique()
df_report_main_text <- df_known_patients |>
dplyr::select(sample,
SYMBOL,
rownames,
HGVSp,
HGVSc,
Consequence,
genotype,
Inheritance,
gnomAD_AF,
ACMG_total_score
) |>
arrange(SYMBOL, sample,
desc(ACMG_total_score),
sample)
# Add a new column with the maximum ACMG_total_score for each SYMBOL
df_report_main_text <- df_report_main_text %>%
group_by(SYMBOL) %>%
mutate(max_score = max(ACMG_total_score)) %>%
ungroup()
# Arrange the data frame based on sample, then descending max_score, SYMBOL, and then by desc(ACMG_total_score)
df_report_main_text <- df_report_main_text %>%
unique() %>%
arrange(sample, desc(max_score), SYMBOL, desc(ACMG_total_score))
colnames(df_report_main_text)[colnames(df_report_main_text) == 'ACMG_total_score'] <- 'ACMG score'
colnames(df_report_main_text)[colnames(df_report_main_text) == 'rownames'] <- 'Variant GRCh38'
df_report_main_text |> nrow()
matches_uniq
View(df_validate)
View(df_borgh_unlifted)
View(df_borgh)
df_long <- read.table(file = "../ACMGuru_singlecase_genetic_df_report.csv", sep = ",", header = T)
df_long <- read.table(file = "../../data/ACMGuru_singlecase_genetic_df_report.csv", sep = ",", header = T)
df_long <- read.table(file = "../../data/ACMGuru_singlecase/ACMGuru_singlecase_genetic_df_report.csv", sep = ",", header = T)
df_long <- read.table(file = "../../data/ACMGuru_singlecase/ACMGuru_singlecase_genetic_df_report.csv", sep = ",", header = T)
df_borgh <- read.table(file="public_borghesi_Supplementary_Table_4.tsv", header = TRUE, sep = "\t")
View(df_borgh)
df_borgh$SYMBOL <- df_borgh$Gene
df_borgh$HGVSp <- df_borgh$HGVS.p
df_borgh$source <- "Borgh"
df_long$source <- "Lawl"
df_long <- df_long |> dplyr::select(SYMBOL, HGVSp, source)
df_borgh <- df_borgh |> dplyr::select(SYMBOL, HGVSp, source)
df_m <- merge(df_borgh, df_law)
df_m <- merge(df_borgh, df_long)
df_m <- merge(df_borgh, df_long, by = c(SYMBOL, HGVSp))
df_m <- merge(df_borgh, df_long, by = c("SYMBOL", "HGVSp"))
df_long$HGVSp |> head()
df_long$HGVSp <- sub(".*:", "", df_long$HGVSp)
# Check the first few entries to ensure it worked
head(df_long$HGVSp)
# Check the first few entries to ensure it worked
head(df_long)
df_m <- merge(df_borgh, df_long, by = c("SYMBOL", "HGVSp"))
View(df_m)
df_borgh |> unique()
df_m <- merge(df_borgh, df_long, by = c("SYMBOL", "HGVSp")) |> unique()
View(df_m)
df_m
paste(df_m$SYMBOL, df_m$HGVSp, sep=" ")
# Concatenate all entries in 'gene_variant' with a comma and print
cat(paste(df_m$gene_variant, collapse=", "), "\n")
df_m$gene_variant <- paste(df_m$SYMBOL, df_m$HGVSp, sep=" ")
# Of the variants that pass QV filters, 17 reported in our main text table with new evidence ACMG score.
df_m$gene_variant <- paste(df_m$SYMBOL, df_m$HGVSp, sep=" ")
cat(paste(df_m$gene_variant, collapse=", "), "\n")
